[project]
name = "mesh-to-ply"
version = "1.0.1"
requires-python = ">=3.10"
dependencies = [
    "meshio>=5.3.5",
]

[project.scripts]
mesh-to-ply = "mesh_to_ply.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"