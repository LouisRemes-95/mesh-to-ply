[project]
name = "mesh-to-ply"
version = "1.0.8"
requires-python = ">=3.10"
dependencies = [
    "meshio>=5.3.5",
    "rich>=14.3.3",
]

[project.scripts]
mesh-to-ply = "mesh_to_ply.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
